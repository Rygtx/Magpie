# 0.6.7 分发准备 / 0.6.7 Distribution Preparation

`0.6.7` 已合并到开发主线 `experimental`。当前准备 `v0.6.7-experimental` 的正式分发文件，内容见 [0.6.7 更新说明](RELEASE_NOTES_v0.6.7-experimental.md)，分发结构沿用 0.6.6。

此处及 `version.json` 是本地发布准备元数据，尚未发布，也未推送更新入口。GitHub Draft、附件上传与发布仍按发布流程分别执行；硬件验收和第三方再分发审核不以构建通过代替。

---

The `0.6.7` branch has been merged into the `experimental` development mainline. Distribution files are being prepared for `v0.6.7-experimental`; see the [0.6.7 notes](RELEASE_NOTES_v0.6.7-experimental.md). Asset layout follows 0.6.6.

This document and `version.json` are local preparation metadata. The release is not published and the update feed has not been pushed. Creating a GitHub draft, uploading assets and publishing remain separate workflow stages; successful builds do not replace hardware validation or third-party redistribution review.
