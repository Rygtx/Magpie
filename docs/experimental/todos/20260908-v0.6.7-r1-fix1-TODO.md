# 067 r1 fix1 todo

日期：2026-09-08。
来源：用户实测 r1 后提出的效果器选择器修订。
基线：`0.6.7` 分支，`75e02786029f29cbaf3ee7369a2f055a6159a253`；已部署至现有 `067 local`。
状态：已按用户指令执行，完成产品代码、Release 构建与自动化回归；实际 UI／多显示器／GPU 体验由用户测试。部署状态以固定运行目录的 build-manifest.json 与版本目录的 deployment-state.json 为准。

## 1. 用户要求与保留行为

- 选择器允许超出 Magpie 主窗口，按显示器可用空间摆放。
- 保留左侧分类、右侧效果器、底部通栏说明的结构；视觉回归 Magpie 主体的圆角、主题与材质，参考图仅用于树形结构。
- 展开和收起标记改为加号、减号，避免切换时视觉跳动。
- 定位并修复列表中不规则空行。
- 同族效果器进一步折叠，例如 CuNNy、NNEDI3；右侧提供 `A–Z／#` 快速定位。
- 具体效果器仍单击立即添加、追加效果组末尾。搜索仍位于右列顶部，详情仍横跨两列底部。
- RTX Video 参数仅在参数按钮与工具栏中展示，新添加默认中档；本轮列表分组不改变效果器 ID、处理参数或配置。
- 下文的家族层级、搜索展开和索引响应方式是本轮建议实施规则，区别于上面的用户明确要求。

## 2. 已定位的问题与证据

### 2.1 弹出范围受到双重限制

`ScalingModesPage.cpp:192–194` 根据 `XamlRoot().Size()` 设置宽高，最大 820×640 DIP，并扣减主窗口边距；窗口缩小会连带压缩选择器。

`ScalingModesPage.EffectPicker.cpp:281–287` 创建普通 `Flyout`，未设置 `ShouldConstrainToRootBounds(false)`。无窗口 XAML 探针实测该 Flyout 默认值为 `true`。官方接口说明支持独立于主窗口边界的桌面弹出层，首次显示前设置即可；不能只提高 Presenter 的最大尺寸。[Microsoft：Flyout 边界属性](https://learn.microsoft.com/en-us/uwp/api/windows.ui.xaml.controls.primitives.flyoutbase.shouldconstraintorootbounds?view=winrt-26100)

### 2.2 配色绕开现有主题资源

`ScalingModesPage.EffectPicker.cpp::_UpdateEffectPickerColors` 使用固定的蓝灰背景、分隔线和青蓝选中颜色；三个不透明色块覆盖了弹出层材质。Presenter 样式也没有显式基于项目已有的 `DefaultFlyoutPresenterStyle`，需要核对并沿用现有模板。

当前工程实际使用 XAML Islands、`Windows.UI.Xaml` 和 `Microsoft.UI.Xaml 2.8.7`；`App.xaml` 已加载 WinUI 控件资源。这里按用户所指的 Magpie 主体视觉统一，无需迁移 UI 框架。`XamlWindow.h:95–99` 在支持的系统上为主窗口设置 Mica。

材质建议：主窗口沿用 Mica，选择器采用标准 Flyout 的圆角、阴影和 Acrylic（亚克力）资源。Mica 主要用于长期存在的窗口背景；弹出层采用 Acrylic 与主体属于同一套 Fluent 视觉体系。透明效果不可用时由主题提供实色回退。[Microsoft：Mica](https://learn.microsoft.com/en-us/windows/apps/design/style/mica)、[Microsoft：Acrylic 弹出层](https://learn.microsoft.com/en-us/windows/apps/design/style/acrylic)

### 2.3 不规则空行：隐藏条目仍累积 StackPanel 间距

已核对内置目录：155 条名称和摘要均非空，且没有换行字符。

生产实现先创建全部行，外层 `StackPanel results` 设置 `Spacing(2)`（`ScalingModesPage.EffectPicker.cpp:223–224`）；筛选时仅将未匹配行改为 `Visibility::Collapsed`（同文件第 357 行）。当前 Windows XAML 会继续为这些隐藏子元素累计 Spacing，因此不连续匹配之间出现异常空白。

已通过独立的原生 XAML 进程复现，同样使用 `StackPanel → Grid`，两条可见行各高 50 DIP，中间插入隐藏行：

| 中间隐藏条目 | Spacing | 两条可见行之间的实测间隔 |
| --- | --- | --- |
| 0 | 2 | 2 DIP |
| 1 | 2 | 4 DIP |
| 10 | 2 | 22 DIP |
| 100 | 2 | 202 DIP |
| 0／1／10／100 | 0 | 0 DIP |

这已定位一条能直接造成用户所述空行的布局缺陷。左侧分类同样使用 `StackPanel.Spacing(2)` 与隐藏子分类，需要同步修复。实际界面是否还存在其他留白问题，修复后由用户复测。

调查产物位于工作区 `.tools/effect-picker-r1-fix1/CollapsedSpacingProbe.cpp` 与 `collapsed-spacing.log`。探针没有显示窗口、启动 Magpie 或运行效果器。

### 2.4 右侧目前没有家族层级

右侧按名称排序并直接展示每个效果器。历史内容 review 已有 `family`，但生成后的产品目录只把它拼入搜索文本，`EffectPickerEntry` 没有可供分组的家族字段。

现有文件分布显示 CuNNy 20 项、CuNNy2 9 项、NNEDI3 10 项，确实容易占满列表。文件夹只用于这次数量核对，不能直接作为全部家族的分组规则，例如 `Sharpen`、`Pixel Art` 包含不同算法。

## 3. 待办：弹出范围与原生样式

- [x] 创建 Flyout 时、首次 ShowAt 前设置 `ShouldConstrainToRootBounds(false)`；保留同一个 XamlRoot、添加目标和已有异常处理。
- [x] 首选尺寸暂沿用 820×640 DIP，实际最大尺寸按按钮所在显示器的工作区及对应 DPI 计算，包含边框、阴影和安全边距。主窗口小于选择器时仍可正常展开。
- [ ] 交由标准 Flyout 完成锚点与边缘避让；核对屏幕四角、任务栏、多显示器、负坐标和不同 DPI，弹出内容保持在可用屏幕范围。
- [ ] 沿用已有窗口移动／DPI 变化时的 Popup 处理，检查重新定位或关闭后重开行为；Esc、点击外部和添加后关闭都应正确清理目标并恢复焦点。
- [x] Presenter 显式沿用 `DefaultFlyoutPresenterStyle`，只调整必要尺寸与内边距；保留系统模板的圆角、边框、阴影、背景材质。
- [x] 移除选择器独立的十六进制配色，采用当前 WinUI 资源中的背景、弱分隔线、文本、选中、悬停与焦点资源。主题资源需能响应运行中的主题切换。
- [x] 左列、右列、底部通过轻微明度层次、边界和留白区分，避免三块强对比颜色；使用一个完整的弹出层材质，内部内容层保持可读性。
- [x] 外边缘形成完整圆角矩形，内部面板背景不盖住圆角；分类与条目的圆角、字号和密度沿用现有控件尺度。
- [ ] 核对浅色、深色、系统强调色、高对比度和关闭透明效果后的回退。技术上不将 Mica 直接作为任意子控件画刷，也不为此引入独立的可交互顶层工具窗口。

## 4. 待办：固定的加减标记

- [x] 左侧分类以及右侧家族统一为收起 `＋`、展开 `−`。
- [x] 使用固定尺寸的图标槽与点击区域，两种状态保持同一中心、横线长度、笔画粗细和文字起始位置；优先复用一条横线，只切换竖线可见性，避免字体基线差异。
- [x] 切换图标不执行旋转或位移动画，行高、数量列及标签位置保持不变。子项展开带来的正常内容位移保留，但维持触发项的滚动锚点。
- [x] 沿用左侧规则：点名称筛选并展开，点加减号只展开／收起；保留键盘左右键与无障碍展开状态。

## 5. 待办：消除隐藏行空白

- [x] 删除两侧依赖隐藏子元素的外层 `StackPanel.Spacing`；条目间距仅附着于实际可见行，例如使用行容器 Margin，或按可见结果建立列表。
- [x] 右侧家族折叠实现采用明确的“可见行序列”（家族标题／效果器叶子），避免继续为全部隐藏效果器创建占位间隔。按稳定 ID 绑定点击和详情，不将可见索引误当原始效果器索引。
- [x] 保留正常的行内名称／摘要间距；自定义条目摘要为空时，摘要元素折叠且不保留空白文本行。
- [x] 添加有意义的布局回归：可见项之间隐藏 0／1／10／100 项、开头／末尾隐藏、全部隐藏、单项结果；检查间隔与滚动范围不随隐藏数量增加。
- [x] 覆盖左侧多个主分组展开与收起，以及右侧搜索／切分类／折叠家族后的排列和焦点；零结果使用现有提示，不显示空占位行。

## 6. 待办：同族效果器折叠

### 数据规则

- [x] 从 review 到产品目录再到 `EffectPickerEntry`，显式保存稳定的家族 ID、显示名和简短家族说明；核对真正的算法家族，避免根据文件夹或名称前缀误合并。
- [x] CuNNy、NNEDI3 为首批必覆盖项；同时 review Anime4K、RAVU、SMAA、xBRZ、FXAA、FSRCNNX 等有多个变体的家族，防止只处理用户举例。
- [x] 建议将 CuNNy 与 CuNNy2 归入 CuNNy 总家族，内部按原版／CuNNy2 保留代际区分；仅在有足够变体时显示第二层，最多两层家族结构。NNEDI3 展开后直接列出变体。
- [x] 用途分类优先：家族只收纳当前分类／搜索条件下的匹配成员，标题显示匹配数量。不会因展开家族带入不相关用途或未匹配效果器。
- [x] 未归类的自定义效果器以原条目保留；缺少家族信息时使用单项展示，避免猜测归属。
- [x] 家族分组只影响浏览，同族变体仍保留各自 ID、名称、参数和用途说明；本轮不将 CuNNy／NNEDI3 重写成参数化效果器。

### 交互规则

- [x] 普通浏览时，多成员家族默认收起，显示家族名、数量、最多一行摘要；唯一成员直接显示可添加效果器，减少无意义点击。
- [x] 点击家族行或加减号只展开／收起；点击具体效果器才添加。家族标题不会代替用户选择某个默认变体。
- [x] 悬停／键盘聚焦家族时，底部显示共同用途与变体区别；聚焦叶子时显示具体效果器说明。
- [x] 家族及独立条目按名称排序；变体采用自然数字排序，使 2、4、8、16 等规格顺序合理，避免纯字符串顺序。
- [x] 普通浏览记住当前页面内的家族展开状态；收起时保留家族标题的滚动位置，焦点位于被收起子项时回到标题。
- [x] 搜索仍覆盖全部分类与别名：临时展开有匹配项的家族，只显示匹配叶子，让结果直接可见；用户可手动收起当前结果。清空搜索后恢复普通浏览的展开状态。
- [x] 键盘上下键仅遍历可见行；家族 Enter／Space 展开或收起，叶子 Enter 添加；左右键在家族与子项之间操作，保持明确的无障碍名称和状态。

## 7. 待办：右侧 A–Z／# 快速定位

建议将“快速选择”实现为当前结果内的跳转入口，不改变用途筛选或搜索关键词，也不直接添加效果器。

- [x] 在效果器列表最右侧提供 `A–Z／#` 索引区域，位于列表范围内，不占底部通栏详情空间。
- [x] 高度充足时显示两列紧凑字母栏（每个目标 24 DIP，14 行）；高度不足以容纳可用点击区域时，显示 `A–Z #` 按钮，点击展开完整字母网格。保持字母可读、可点，不把 27 个目标硬压进过短列表。
- [x] 英文字母不区分大小写；数字、符号及其他非 A–Z 开头的名称归入 `#`。按去除首尾空白后的显示名称确定索引，`#` 放在 Z 后并与实际排序一致。
- [x] 有成员的家族按家族名称索引，独立效果器按自身名称索引。点 C 定位 CuNNy 家族标题，点 N 定位 NNEDI3；家族内变体通过展开和搜索查找。
- [x] 索引根据当前分类或搜索结果更新；无匹配结果的字母弱化且不可激活，点击有效字母滚动并聚焦首个对应家族／单项。
- [x] 在完整范围中保留同一字母的其他条目，索引动作本身不执行过滤；搜索状态下只跳转搜索结果。
- [x] 跳转滚动使用布局后的真实位置或控件的滚动定位能力，不用行号乘固定高度；展开、折叠和摘要变化后仍指向正确条目。
- [x] 支持键盘进入索引、选择字母和返回结果；字母输入不抢占搜索框输入，索引与滚动条互不遮挡。


## 8. 执行顺序与验收

建议顺序：先修空行与弹出边界，再统一主题和加减标记，接入家族可见行模型，最后增加字母索引并联合验证。

- [x] 核对当前分支与部署基线、产品目录字段、主窗口材质与 Flyout 使用方式。
- [x] 原生 XAML 复现隐藏条目累计 Spacing，并确认 Flyout 默认边界限制。
- [ ] 小主窗口下选择器可延伸到窗口之外，仍能完整操作；屏幕边缘与多 DPI 下定位正确。
- [ ] 深浅主题下圆角、材质、选择、悬停、焦点与 Magpie 主体协调，三个区域仍清楚。
- [ ] 展开／收起时图标与标签不跳动，焦点和滚动位置稳定。
- [ ] 分类和搜索结果无不规则空白，隐藏项目不增加内容高度；原有点击添加闪退与参数绑定回归保持通过。
- [ ] CuNNy／NNEDI3 等家族折叠、搜索临时展开、计数、自然排序和叶子添加对应正确 ID。
- [ ] A–Z／# 在全部、用途分类、搜索、无结果和小尺寸下均可用；同族展开后跳转位置仍正确。
- [ ] 仅浏览、搜索、展开和悬停不触发效果器初始化；保持已有启动前保护。
- [x] 实施后的 Release 构建及相关模型／原生布局回归通过；实际 Magpie 交互与视觉由用户测试，沿用“我自己测试”的偏好。
- [x] 按用户要求复用 `release/v0.6.7-local/Magpie-Experimental-x64`；构建与运行包合一，中间文件在同版本 `obj/`，fix1 不新增运行目录。最终提交和部署状态以发布清单为准。

## 9. 实施补充（2026-09-08）

- 明确审阅 12 个算法家族，来源见 `reviews/20260908-effect-family-review.json`；CuNNy 包含两代子组。保留 155 个正式效果器 ID，未将同族算法改成参数化实现。
- 使用当前可见行模型，家族标题与叶子使用不同键。搜索、分类、展开与字母定位均不加载效果器或初始化 GPU。
- 常用尺寸显示两列 A–Z／# 索引；工作区高度不足 336 DIP 的列表改用按钮和完整字母网格，避免原先单列 27 项在首选尺寸下无法放下。
- 保留 XAML Islands／WinUI 2.8.7 框架，沿用 `DefaultFlyoutPresenterStyle`，以 ThemeResource 响应主题；没有照搬参考图的蓝灰／青色配色。
- 原生无窗口回归覆盖隐藏行高度、加减号几何、深浅主题资源、默认模板、家族筛选和 ID、排序、索引、参数绑定与旧配置迁移。脚本为 `tests/Run-EffectPickerFix1Tests.ps1`，诊断日志放在工作区 `.tools/effect-picker-r1-fix1/`。
- 根据用户本轮修正，移除 `release/.build` 部署约定，直接复用固定运行目录；源码／发布／交接文档同步。Conan 生成配置与 WinUI 提取缓存也使用版本 `obj/`。`Build-Release.ps1` 的正式包同样直接在版本发布目录构建；该脚本拒绝清空 `067 local`，本轮未运行正式 ZIP 打包或任何 GitHub 发布操作。
- 屏幕四角、多显示器 DPI、透明效果关闭／高对比度、鼠标和键盘交互的最终体验留给用户复测，以上未实测项仍保留未勾选状态。

## 10. 用户后续调整：DLSS SR 定位与面板密度

- DLSS SR 改回「放大与超分 → 时序重建」，与 FSR、XeSS SR 同组；名称、用途、推荐理由和组合位置同步。保留输入条件、默认模型和用户反馈的限制说明，效果器 ID 与后端执行不变。
- 首选弹窗高度 640 → 800 DIP，底部说明高度 192 → 144 DIP；新版上下比例 82:18，屏幕工作区限制保留。上方旧尺寸为 fix1 初始实施记录，本节取代其当前尺寸约定。
- 面板内文字统一减小 1 DIP，保留按钮内边距与字母索引的固定点击尺寸。原生布局回归覆盖新增首选尺寸及说明区实际高度；内容检查确保 DLSS 与 FSR2／3／4、XeSS SR 归属一致。
- 继续使用固定 `067 local` 目录覆盖部署，实际界面由用户自行测试。

## 11. 用户后续调整：进阶入口、完整说明与三区层次

- 「推荐入门」更名为「入门」，保留原筛选；新增独立的「进阶」快捷分类，精确包含 RTXVideo VSR、DLSS FG、XeSS FG 两个版本、DLSSNR、Frame Rate Filter。它与 review 的“进阶／实验”推荐级别分开，原用途入口和保存 ID 保留。
- 初始说明高度仍为弹窗的 18%（800 DIP 时为 144）。按全文实际换行测量标题、正文、间距与提示，超过初始高度时向上扩展，同时压缩两列；切回短说明或重新打开时恢复初始高度。默认为上方保留 180 DIP，空间不足时采用滚动兜底。
- 合并同一次输入中的测量请求并延后布局；实际鼠标移动与键盘聚焦才切换说明，调整后保持来源条目可见，避免静止指针下的条目变化触发反复展开。
- 分类／效果器／说明采用系统主题基色的 2.5%／6%／10% 覆盖，效果器区域处于两侧之间，保留原生材质、圆角和主题切换。
- 回归覆盖 6 项精确分类、全部 155 项说明在两种宽度下的换行、长短切换、可用空间上限和深浅主题的三区明暗顺序。实际交互与视觉仍由用户复测，继续覆盖固定 `067 local`。
