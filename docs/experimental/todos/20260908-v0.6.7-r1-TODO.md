# 067 r1 todo

日期：2026-09-08。
来源：最近两轮关于 DLSSNR 自动适配、RTX Video 参数化、效果器选择器布局与分类树的对齐讨论。
基线：`0.6.7` 分支，`3c56c7110883d19ecfad58e660c4606e72731e93`。
状态：功能实现与自动化回归已完成；真实 UI／NVIDIA GPU 验收保留给用户。发布状态以 `067 local` 的 `build-manifest.json` 为准。

## 1. 目标与优先规则

- DLSSNR 的 HDR 处理根据输入自动适配，减少需要用户理解的协议参数。
- RTX Video 改为固定效果器 ID 加真实档位参数，新添加默认中档。
- 效果器选择器采用左侧分类树、右侧效果器列表、底部通栏说明的三个区域。
- 以后一次讨论为准：分类采用参考图中的树形展开结构；RTX Video 默认中档，替代此前的低档方案。
- 保留已经确定的添加方式：单击立即添加、默认追加效果组末尾。
- 下文将用户已明确的要求与建议方案、待核实技术问题分别记录；实验倍率、无中断切档等尚不能视为已验证能力。

## 2. DLSSNR：HDR 参数自动适配

### 待执行

- [x] 将截图中的 `HDR Processing Path` 和 `Experimental HDR Scale` 改为内部自动处理，从参数按钮面板和工具栏参数列表中移除这两个手动控件及空分组。
- [x] 根据实际输入颜色信息、当前 HDR 管线及后端能力决定处理路径，在对应资源创建前完成必要的路径判定；纹理格式本身不足以说明 HDR 语义。
- [x] 输入归一化与输出逆转换使用一致的颜色信息和参数，保持亮度关系；梳理输入或颜色信息改变时的更新与资源重建行为。
- [x] 读取旧配置时，将这两个旧手动参数转换为自动策略，保留其他风格、细节和运动参数。
- [x] 记录自动选择的路径及必要诊断信息；需要用户处理的异常给出明确下一步。

### 建议方案与待核实项

- [x] 首版采用“自动路径＋中性倍率 `1`＋已有白点归一化”；使用生产转换着色器验证数值往返，模型画质另行实测。
- [x] 将旧 `1 / 2 / 4.5` 实验倍率退出用户配置；归一化依据为当前 SDR 白点与 scRGB 的 80 nit 参考值，不猜测最佳倍率。
- [ ] 验证 FP16 实验路径、输入信息不完整时的处理策略，以及 HDR 路径下原有细节参数的适用范围。
- [x] 区分自动选择路径、根据颜色信息归一化和逐帧画面分析；本轮没有确定新增逐帧亮度分析算法。

## 3. RTX Video：真实参数化

### 效果器与参数

- [x] 保留两个用途独立的正式效果器：`RTXVideo_Denoise`、`RTXVideo_VSR`，各自使用固定 ID；建议完整 ID 为 `RTXVideo\\RTXVideo_Denoise`、`RTXVideo\\RTXVideo_VSR`。
- [x] 为每个效果器定义真实档位参数，选项为低／中／高／极高，修改时只更新参数。
- [x] 新添加默认中档；在下拉列表选择“中”恢复默认档位。现有配置继续保留原档位，未新增单独重置按钮。
- [x] 复用现有底层处理实现，重构效果器定义、后端档位读取、参数通知和持久化通路，移除当前用替换旧效果器 ID 实现强度控制的过渡逻辑。
- [x] 保持 Denoise 的同分辨率处理规则与 VSR 的输出缩放规则。

### 参数入口与运行时更新

- [x] 添加效果器阶段只显示效果器名称与用途，移除强度选择控件。
- [x] 效果组行通过参数控制按钮显示强度；弹层保留简洁标签与选项，删除冗长操作说明。
- [x] 工具栏参数面板提供同一项强度控制，与设置页使用一致的参数定义、取值、默认值和保存规则。
- [x] 接入实际运行中的档位应用，区分待应用、已应用和失败状态，避免仅更新界面或配置。
- [x] 按 SDK 参数更新可能需要重载的接口约定，显式加载候选模型。未将无中断切换作为已验证能力；实际加载时延留待用户验收。
- [x] 若需要重新加载，设计后台安全应用流程及简短状态反馈；失败时恢复原档位并给出可执行的提示。

### 旧配置兼容

- [x] 对八个已知旧 ID 建立精确迁移映射，配置读取和效果组导入走一致逻辑。

| 旧 ID 后缀 | 新效果器 | 保留档位 |
| --- | --- | --- |
| `RTXVideo_Denoise_Low / Medium / High / Ultra` | `RTXVideo_Denoise` | 低／中／高／极高，分别对应 |
| `RTXVideo_VSR_Low / Medium / High / Ultra` | `RTXVideo_VSR` | 低／中／高／极高，分别对应 |

- [x] 迁移保留效果器顺序、缩放设置和其他参数；再次加载不重复改变配置。
- [x] 迁移前保留备份，正常保存及导出使用新 ID 和参数；检查迁移与现有配置校验、修复的执行顺序。
- [x] 旧效果器名称继续作为搜索别名；确定旧文件的过渡兼容和发布包清理方式，避免出现重复候选项。
- [x] 更新内置效果组、效果目录、用途说明、HDR 路由识别和相关映射，消除正式运行路径对旧档位名称的依赖。

## 4. 效果器选择器：三分区布局

- [x] 第一列为分类树，第二列为效果器列表，底部为横跨两列的完整说明区。
- [x] 搜索框只放在右侧效果器列顶部；继续搜索名称、用途、中文关键词和旧名称，默认覆盖全部分类。
- [x] 效果器条目保留名称及最多一行用途说明，超出部分省略；详细推荐程度、组合位置、使用条件和必要 HDR 注意事项移到底部。
- [x] 左列、右列、底部采用有明显明度差的主题背景，加竖向和横向细分隔线；深色、浅色主题分别适配。
- [x] 分类选中、条目悬停和键盘焦点具有可区分的视觉反馈。
- [x] 删除“添加效果器”标题，以及“单击效果器追加到末尾／悬停查看说明／添加后可拖动调整顺序”等固定操作说明，清理重复提示。
- [x] 分类树、效果器列表和长说明独立滚动，保持分区尺寸稳定；适配小窗口与显示缩放。
- [x] 悬停或键盘聚焦分类、效果器时更新底部内容，移开后保留最后查看的说明，方便阅读。
- [x] 初始底部可显示当前分类说明，避免以固定操作教学占用空间。

布局参考：

```text
┌─────────────────┬────────────────────────────┐
│ 推荐入门        │ 搜索效果器……               │
│ 全部            ├────────────────────────────┤
│ ▾ 放大与超分    │ 效果器名称                 │
│   ├ 通用画面    │ 一行用途说明……             │
│   ├ 动画与线条  │                            │
│   └ 像素画      │ 效果器名称                 │
│ ▸ 清理与修复    │ 一行用途说明……             │
│ ▸ 抗锯齿        │                            │
│ ……              │                            │
├─────────────────┴────────────────────────────┤
│ 当前分类／效果器名称                         │
│ 用途、适用场景、组合位置、条件及必要说明     │
└──────────────────────────────────────────────┘
```

宽度约 25% 分类／75% 效果器、底部高度约 30% 是上一轮布局建议，具体按内容和可用尺寸调整。

## 5. 分类树：按用户参考图完善展开结构

用户参考图重点：缩进层级、细层级线、明确的展开标记、整行选中背景、右对齐数量。主分组和子分组保留在同一列，替代上一轮将子分组移到右侧筛选栏的建议。

### 待执行的交互方案

- [x] 点击主分组名称：筛选整组效果器，并展开子分组。
- [x] 点击展开箭头：只展开或收起，保留当前筛选。
- [x] 点击子分组：筛选该子分组。
- [x] 支持多个主分组同时展开，记住展开状态和滚动位置。
- [x] 层级缩进、引导线、箭头与点击区域保持一致；分类右侧显示数量，按合并后的实际候选效果器计算。
- [x] 区分展开状态、当前选中分类和悬停状态；检查收起含当前选中子项的主组后，筛选状态仍可辨认。
- [x] 支持键盘导航、展开和收起，以及对应说明查看。

## 6. 验证与交付待办

- [x] RTX Video：自动化验证新添加中档、四档数值、八个旧 ID 迁移、参数／尺寸保留与重复迁移；配置读取和导入共用此逻辑。实际界面重开／导入导出留待用户验收。
- [ ] RTX Video：参数按钮与工具栏数值同步，实际后端应用结果可确认；重载或失败流程保持状态一致。
- [ ] DLSSNR：覆盖 SDR／HDR 输入、旧手动参数、无效或缺失颜色信息、输入变化，检查自动路径和归一化／逆转换一致性。
- [ ] 选择器：确认三个区域、树形展开、单行摘要、全局搜索、通栏详情、小窗口及键盘行为。
- [x] 原生 XAML 回归确认旧集合抛 E_INVALIDARG、新集合四档选择正常，生产三分区布局在三种尺寸下构建与测量通过；实际添加按钮点击由用户验收。
- [x] 更新效果目录与相关校验，155 个源码与目录条目一致，正式候选去重；嵌入与发布文件由部署核验记录。
- [x] 完成原生／配置回归及 Release 验证构建；发布脚本逐文件核对清单和 SHA-256，最终结果写入版本目录的构建清单。
- [x] 实际 Magpie 界面和 GPU 画质测试由用户执行，沿用“我自己测试”的偏好；单独记录未验证项。

实施依据：用户于本轮要求开始执行。部署沿用此前明确的 `067 local` 覆盖约定；r1 为本轮功能修订名，尚未另建 `067 local r1` 运行目录。

详细行为、验证方法与待用户实测项见 [r1 使用与验证记录](../testing/EFFECT-PICKER.md)。

## 7. 本轮实施决定与验收边界

- 首版采用自动路径＋倍率 1＋已有白点归一化。旧实验倍率不再参与参数选择；不新增逐帧亮度分析。
- SDK 参数设置后可能需要重新加载；本轮显式加载候选模型，后台串行替换，不承诺零停顿。候选失败保留原模型；状态与配置只回退属于该请求的值。
- 旧配置迁移代码、选择器布局、参数入口和发布旧文件备份处理已实施。
- 未完成的 GPU／UI 项目由用户按偏好自行验收；代码判定、软件 WARP 数值验证和原生 XAML 布局测试不能替代真实模型画质验证。

## 8. 已完成的自动验证

- Release x64：通过。
- 搜索／分类与 RTX 名称、八个档位映射：通过。
- RTX 精确迁移、幂等性、无效值回退、保留其他参数／尺寸、旧失败请求不覆盖新值：通过。
- DLSSNR：HDR 开关、FP16 能力、canonical 输入、SDR／PQ／HLG 已转换输入与无效颜色数据的判定通过。
- 独立 XAML 进程：集合绑定与三分区生产布局（820×640／480×360／280×220）通过。
- 软件 WARP：54 组原有 HDR 转换回归、3 组自动 DLSSNR 白点数值往返通过；未执行 NVIDIA 模型。
- 原有全屏源窗口、效果组／捕获条件前置保护：通过。
- 日志位于工作区 `.tools/effect-picker-r1`；发布文件及提交号以 `release/v0.6.7-local/Magpie-Experimental-x64/build-manifest.json` 为准。
