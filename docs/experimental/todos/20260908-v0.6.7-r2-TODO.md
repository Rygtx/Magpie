# 067 r2 TODO

日期：2026-09-08。基线：`0.6.7` / `14f5ead64f8dcd79dfa246e9c5d932417e1cee33`。

状态：**调查和规划完成，待用户对齐后执行。** 当前没有产品代码变更或覆盖部署。详见 [DLSS 管线与官方文档 review](../reviews/20260908-v0.6.7-r2-dlss-pipeline-review.md)，含代码定位、官方固定版本链接和时钟探针结果。

`r2` 用于本轮任务与文档命名。后续部署继续覆盖既有 `067 local`，构建/中间文件沿用 [本地约定](../GIT-WORKFLOW.md)，不创建新运行目录。

## 1. 范围与结论

- 检查并优化光流 → Frame Guidance 共享资源 → DLSSNR / DLSS FG 的实际消费链。
- 对照官方 NGX 310.7.0、Streamline 2.12.0、Reflex / NVAPI，重点处理帧队列、间隔、排序和低延迟。
- 官方差异清单不包含估算运动与引擎运动的差别、置空深度输入的差别；本轮保持这些输入合同。不新增深度估算、原生 jitter 或源应用注入功能。
- DLSSNR 仍为 AI 风格重塑；保留现有参数、残差顺序、HDR 自动适配和实时编辑语义。
- GPU / 实际操作仍由用户测试；自动检查只证明对应逻辑、资源合同和构建结果。性能收益目前没有实测承诺。

调查结论：**共享提供器、同帧去重和基本帧排序已经存在；主要机会在多余通道/尺寸转换、FG 同步等待、颜色复制、帧节奏与 Reflex。** 不安排重复实现已有共享，也不把未接入 Streamline 等同于直接 NGX 不合规。

## 2. 执行顺序

| 顺序 | 工作项 | 等级 | 产物 |
| --- | --- | --- | --- |
| 1 | R2-01 性能与帧身份基线 | P0 | 统一追踪、可比较的测量表 |
| 2 | R2-02 等待/退役/停用边界 | P0 | 安全推进异步处理的基础 |
| 3 | R2-03 帧间隔与时间来源 | P0 | 消除短间隔追赶，明确调度时钟 |
| 4 | R2-04 队列年龄和插帧淘汰 | P1 | 有界积压与真实帧优先保证 |
| 5 | R2-05 按需生成 confidence | P1 候选 | 无消费者时减少反向场、cost、稠密输出 |
| 6 | R2-06 引导适配复用 | P1 候选 | 同尺寸快速路径、共享等价转换 |
| 7 | R2-07 FG 命令/输出/标志槽 | P1 候选 | 异步完成发布，减少 CPU 阻塞 |
| 8 | R2-08 颜色共享与复制削减 | P1 候选 | 减少 NR、FG、发布之间的数据搬运 |
| 9 | R2-09 Reflex 原型与验收 | P1 调查后实现 | 多设备标记方案、状态/延迟验证 |
| 10 | R2-10 Streamline 路线比较 | P2 决策项 | 明确迁移收益、成本及采用条件 |

候选项先测对应开销、做小范围验证，再决定是否进入产品路径。P0 优先；R2-07/08 依赖 R2-02，R2-09 依赖 R2-01 的帧身份。每项可单独提交、验证和回退。

## 3. 待执行清单

### R2-01：建立完整的性能和帧身份基线

- [ ] 将 `{captureSequence, resourceGeneration, frameId, interpolationIndex, generatedCount, presentId}` 贯通 FG 输出、共享槽、FIFO 和实际提交。真实帧、插帧、纯 UI 重绘分别计数。
- [ ] 分别记录捕获 timestamp、接受时刻、Evaluate 提交/完成、发布/入队、到期时刻、Present 提交；统一时钟域转换，缺失 timestamp 明确标记。
- [ ] 补齐 FG Evaluate GPU 时间、禁用标志完成等待、输出复制等待；沿用已有 NR timestamp 和 NVOF 查询，避免为了统计引入同步 readback。
- [ ] 补齐 NR 颜色预处理、guidance 适配、残差、HDR 边界转换和复制的 GPU 时间；CPU submit 时间与 GPU 时间分列。NVOF 现有查询覆盖的范围与 OF 硬件引擎实际工作用 GPU trace 交叉核对。
- [ ] 记录 adapter 命中/分配、OpenSharedHandle、Flush、复制次数和按格式估计的数据量、槽占用与队头年龄。
- [ ] 修正/替换当前 FG `paceWait` 指标：代码在成功尝试里令结束时间等于开始时间，之前多次 Retry 的等待没有累计，不能把该值近零解释为没有 pacing 成本。
- [ ] 建立 NR-only、FG-only、NR+FG、SR→NR+FG 的可重复采样表，记录分辨率、倍率、光流档位、HDR、刷新率、VRR/呈现路径、DLL/驱动/代码版本。

验收：能分清 captured、evaluated、queued、submitted、displayed；尚无显示端数据时不得把 queued 或 Present 成功当作实际显示 FPS。输出中保留 p50/p95/p99/max、帧年龄、显存；采样开关自身开销可比较。

### R2-02：先明确等待、资源退役和停用语义

- [ ] 为 FG/NR/共享发布梳理 producer ready、consumer done、copy done、present slot free 的独立含义，画出各队列 Signal/Wait 关系。
- [ ] 统一有界、可响应停止的 CPU 等待；覆盖发布 fence 和互操作换资源等待，处理 device removed。超时后进入明确失败状态，不把未完成纹理交还复用。
- [ ] 用强资源身份及代际校验缓存；在资源轮转/resize 时，按最后消费者 fence 延迟退役共享资源与视图。
- [ ] 将“尚未准备好”和“旧序列/旧代际应丢弃”从前端底图获取一路传到 FIFO；检查当前过期分支返回 false 后重试/旧底图复用的风险。
- [ ] 统一 FG 失败停用的 generator、`_hasFrameGeneration`、FIFO、ring、计时器和计数状态，确认所有槽都能释放，普通真实帧路径正常接管。
- [ ] 覆盖停止缩放按钮、初始化失败、部分 MFG 成功后失败、resize/捕获序列切换、互操作失败的可控故障测试。

验收：旧帧不进入新序列，失败/停止能结束等待，无提前复用和退出死锁；原有创建窗口/GPU 前全屏检查继续通过。真实 GPU 故障不主动制造，故障注入在受控测试层完成。

### R2-03：修正帧间隔与时间来源

- [ ] 修正 `FramePresentationClock` 在迟到 0–1 个周期时可能出现极短间隔的问题。采用有下限的相位修正或重新锚定，参数由固定帧率测试和显示端验证决定。
- [ ] 对比捕获 timestamp 与 backend 接受时间；优先使用已验证有效的源时间间隔，无效/重复/倒退时回退。保持捕获序列重置规则，不把静止窗口的普通投递间隙误判为场景切换。
- [ ] 将 FG deadline 接入 `WaitForFrontendWork` 的可中断定时等待，保留 DXGI 容量门；明确 pacing 和容量等待不是同一个门，也不叠加新的重复限帧。
- [ ] 明确重置、禁用插帧、丢弃过期生成帧后真实帧的目标时刻，避免仍以请求倍率机械计算所有间隔。
- [ ] 添加固定周期、交替长短帧、恰好一个周期迟到、长停顿、倍率/刷新率变化、消息唤醒与停止的时钟测试。

验收：10 ms 周期、迟到 9/10 ms 的旧探针案例不再允许无约束的 1/0 ms 追赶；没有长停顿后连续补交的突发帧。具体显示间隔由用户采样验证。

### R2-04：队列年龄预算与真实帧保护

- [ ] 在现有 2–4 槽有界环上增加队头年龄、目标时刻和晚帧分类，明确预算依据源周期、目标间隔还是显示刷新率。
- [ ] 设计只淘汰已失去显示价值的生成帧的策略，保留同序列中的有效真实帧；旧捕获序列整组丢弃时使用独立原因。
- [ ] 保持 NGX Evaluate 索引连续：显示层跳过某张插帧不等于跳过该索引的 Evaluate。若取消整个帧对，通过合法 reset 在下一个 index 1 开始。
- [ ] 确保第一张就绪的有效生成帧可以先呈现，后续按顺序跟进；发布、跳过和失败全部释放对应资源租约。
- [ ] 用低刷新率/高倍率、慢 NR、慢 FG、慢前端和队列突发的模拟序列检查帧龄、真实帧交付与排序。

验收：顺序保持 `上一真实帧 → 本帧对有效插帧（索引递增）→ 当前真实帧`；积压不会仅靠扩大环容量掩盖，参数实时编辑/工具栏响应不退化。

### R2-05：按消费者需要生成置信度

- [ ] 扩充 Frame Guidance 需求合同，区分 motion、confidence 及诊断用途；允许不读取 confidence 的消费者通过合法视图校验。
- [ ] FG-only / 关闭输入分辨率调整的 NR 等无置信度消费组合，试验单向 OF、关闭 cost、仅输出稠密 motion 的路径。
- [ ] NR 专用引导重采样或置信度诊断启用时保留所需 confidence；同组任何消费者需要它时共享完整结果。100% 同尺寸场景与 R2-06 快速路径联动验证。
- [ ] 比较新旧运动结果、HDR 转换、首帧/重置/降级语义和 NVOF GPU 时间，确认按需优化没有引入视觉回归。

验收：不降低用户选择的光流质量档位；前后向请求变化的图像影响有证据。收益不明显或驱动结果不稳定则保留现状。

### R2-06：引导图尺寸转换与缓存

- [ ] NR 的 100% 同尺寸引导直接复用相应共享视图；保留输入分辨率开关对应的残差控制行为。
- [ ] 评估等价的 NR 专用引导转换能否纳入共享缓存，键至少表达源资源/代际、帧、尺寸、提供器、采样规则及所需通道；不能仅凭目标尺寸合并不同来源或不同核。没有实际复用者时不为缓存而重构，也不扩大现有 signed-snippet 的 NR 实例限制。
- [ ] 对“通用适配 → NR 再降采样”拆分成本。先共享等价转换；若改成从原捕获尺寸直接生成 NR 引导，单列为有画质影响的实验并比较结果。
- [ ] 评估同尺寸 Zero 引导复用、无需通道的分配裁剪、history reset 保留兼容分配、重复 fence wait 去重。

验收：相同转换一帧最多执行一次；NR+FG、SR→NR、None、首帧/重置均保持合同，confidence 加权与已有恒定深度语义不变。

### R2-07：FG 的异步完成与输出槽

- [ ] 将单 allocator/输出改为有界的命令、输出、readback 槽，单独跟踪 Evaluate 完成与 D3D11 读取完成。
- [ ] 复用等待事件；完成标志用 fence 检测，避免主生产路径逐次 CPU 阻塞等待，但仍在读取/决定发布前确认标志有效。
- [ ] 异步发布第一张完成插帧，后续按索引发布；不等待整组 MFG 后才交付第一张。
- [ ] 明确颜色输入和 Frame Guidance 的所有消费者完成后才能覆盖；同帧跨队列可见性与 D3D12 resource state 全部有对应同步。
- [ ] 分别比较 2x/3x/4x 的 CPU 等待、GPU 空档、显存和画面年龄；槽数以证据确定，不盲目增加在途帧。

验收：disabled/reset/失败路径都不发布无效插帧；allocator 与纹理不提前复用；第一张生成帧延迟、p95/p99 和显存达到可接受结果。

### R2-08：NR → FG → 呈现的颜色复制

- [ ] 先尝试 NR 残差最后一个合成 pass 直接写兼容的效果输出 UAV，避免随后整帧 CopyResource；不支持的格式继续回退。
- [ ] 对相邻且兼容的 NR/FG 设计 shared color + metadata + ready fence 交接，核对中间效果、格式、尺寸、HDR 白点和颜色域后才启用。
- [ ] 将发布复制的完成保护从全路径 CPU wait 调整为资源槽/最终消费者的 fence 依赖，依赖 R2-02/07 的完整所有权设计。
- [ ] 核对共享槽、本地底图、呈现目标、稳定底图每次复制的用途；尝试交换底图所有权或延迟保存，保留工具栏、光标、原图对比和纯 UI 重绘。
- [ ] 用 R2-01 指标决定是否继续做跨 API 直通；无需改造的组合保留成熟路径。

验收：至少能明确减少了哪次复制/哪段等待及其代价；HDR、输入分辨率、上游实时参数修订、原图对比与生成帧都保持正确。

### R2-09：Reflex 低延迟原型

- [ ] 形成直接 NGX + 原生 NVAPI 的可行性说明：D3D11 backend、D3D12 NGX、D3D11 frontend 三者如何登记设备/队列，谁调用 Sleep，谁管理呈现标记。
- [ ] 设计严格递增的真实工作帧 ID、异步插帧 present ID 和对应关系；核对 `SetAsyncFrameMarker` / 带外队列接口。标记真实的 Magpie 工作，明确没有源游戏 simulation 信息。
- [ ] 按公开 API 合同实现可关闭原型：能力/版本检查、SetSleepMode、Sleep、状态、延迟查询和完整标记；核对与 Front Edge Sync/捕获限帧/呈现时钟的分工。
- [ ] 在 Magpie 开始本帧有效工作前选择合适 Sleep 位置；不把等待塞在已持有共享槽或资源锁的区域，不按生成倍率重复执行整套模拟帧标记。
- [ ] 分开验证 Off / On / On+Boost；硬件不支持或查询失败有明确状态及回退。默认模式在结果确认后决定。
- [ ] 获取原生 Reflex SDK 验证资料/工具后运行对应检查；使用显示端工具测捕获后附加延迟。不能把 Magpie 的 PCL 标记推导为源游戏点击到显示的完整延迟。

验收：不仅 API 返回成功，还需标记对应正确、延迟指标有效、重复限帧不存在，吞吐量与帧时间有对照。未验证前不在产品中宣称“已支持 Reflex”。

### R2-10：完整 Streamline 路线比较（决策项）

- [ ] 对比“保留直接 NGX、自管呈现、原生 Reflex”与“统一 D3D12 呈现、Streamline DLSS-G + Reflex/PCL”的成本和收益。
- [ ] 若做后者原型，核对代理交换链、资源标签寿命、frame token、异步错误回调、关闭/resize 和资源释放；完整接入 `sl.reflex` 与 PCL。
- [ ] 研究官方硬件节奏、实际呈现计数和 VSync 能力对现有场景的帮助，验证 IFLIP/合成模式、VRR、多显示器和 HDR。
- [ ] Streamline 专用/Vulkan 专用能力不机械加入当前 D3D11/D3D12 直连路径；标记为 Reserved 的 NVAPI 接口不作为实现依据。

验收：给出是否值得迁移的具体判断。此项不是默认的大规模重写授权，也不顺带增加 5x/6x、动态倍率或 Reflex Frame Warp。

## 4. 统一验收与部署

- [ ] 自动逻辑检查覆盖帧 ID、MFG 排序、deadline、晚帧、禁用标志、旧代际、None 与置信度需求组合。
- [ ] 资源变化验证 shader 编译、格式/尺寸/同步合同，必要时使用隔离的软件设备测试；不操作用户 Magpie UI。
- [ ] Release x64 构建与 `git diff --check` 通过；按改动同步测试规范和本 TODO，GPU 验收项保持未勾选直到用户验证。
- [ ] 用户测试矩阵包含 SDR/HDR，NR 原生尺寸与 SDR 100/75/50% 输入，FG 2/3/4x（以设备实际支持为准），组合链、静止/运动/场景切换、停止/恢复、VRR/不同刷新率。
- [ ] 前后比较同时提交性能表和画质/稳定性结果；仅 CPU API 耗时降低而显示延迟上升的改动不判为成功。
- [ ] 后续用户授权执行后，在正常退出 Magpie 的条件下覆盖既有 `067 local`，校验清单并保持配置兼容。

## 5. 本轮调查完成项

- [x] 核对共享提供器、目标尺寸缓存、NR 与 FG 的颜色/引导消费路径。
- [x] 核对直接 NGX、Streamline、Reflex/PCL、NVAPI 官方资料并固定引用版本。
- [x] 确认基本 MFG 顺序、禁用标志、手工保留真实帧及 NeverProvided 优化已存在。
- [x] 确认 Reflex 尚未接入，列出原生与 Streamline 两条路线的不同要求。
- [x] 用当前时钟头文件编译无窗口探针，确认迟到边界的短间隔追赶风险。
- [x] 整理候选项、依赖和验收边界；未修改产品实现，未执行 GPU 性能或实际显示测试。
