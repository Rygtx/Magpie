# v0.6.7 Beta 6 TODO

创建日期：2026-09-10（Asia/Shanghai）。基线：beta5 `ef538ffc`。
状态：P0／P1 代码、无界面回归和完整 Release 构建已完成，P2 评估已记录。实机验证保持待办，不预先宣称性能收益。

上述完成状态仅指原 Beta 6 范围，**不包含 DLSSFG 的真正 Reflex 驱动基础限帧**。新增 Fix1 已完成代码与无界面回归，完整 Release 构建通过，见下节。

实施记录：[Beta 6 修复与验证](../reviews/20260910-beta6-implementation.md)；[Streamline 与资源池评估](../reviews/20260910-beta6-streamline-assessment.md)。

## Fix1：实现 DLSSFG 的真正 Reflex 驱动基础限帧

新增日期：2026-09-10。状态：代码和无界面回归完成；完整 Release 构建通过，实机与性能验证未完成。提交后的包版本／清单和资源核对结果保存在随包 `LOCAL-PREPARATION-AUDIT.json`。实施记录：[Fix1 Reflex 驱动基础限帧](../reviews/20260910-beta6-fix1-reflex.md)。

目标：DLSSFG 选择 Reflex 且驱动路径可用时，由 Reflex 负责基础限帧与低延迟请求；不能继续把“Async 基础限帧＋Reflex 低延迟”视为这一目标已经完成。原 Beta 6 的 Async 方案保留为明确选择和失败回退。此项取代原 P1 中“纯 Reflex 限帧留待以后决定是否开放”的暂缓安排。

### 实现与职责

- [x] 实现可执行的 DLSSFG Reflex 限帧路径，包括模式解析、驱动配置、捕获循环和运行状态；不能只修改名称、文案或把请求映射到 Async。
- [x] Reflex 生效时，统一基础目标仅由驱动限帧负责。移除同目标的 Async／StepTimer 固定限帧和 Front Edge 的 FG 输入期限等待；更低的配置／捕获上限合并为同一个有效目标，不另叠加限帧器。
- [x] 在捕获前，每个基础输入周期调用一次 Reflex Sleep；捕获等待／重复轮询保留候选身份，不在 FG 后或每张生成帧上再加一次 Sleep。保留处理、异步 FG、前端渲染及 Present 的对应标记。
- [x] 明确并实现目标基础 FPS 到驱动间隔的换算，覆盖 2×／3×／4×、手动目标、0 自动目标、刷新率变化及更低上限。核对当前直接 NGX 路径的契约，不未经验证套用 XeLL／Streamline 的倍率换算，也不把生成输出 FPS 当成基础 FPS。
- [x] 保留生成帧／真实帧顺序、独立输出节奏、有界 FIFO、槽位所有权、捕获前背压和必要 fence 等待。启用驱动基础限帧不等于删除输出调度或资源同步。
- [x] 分别判断接口可用、驱动限帧请求结果与实际低延迟状态。`bLowLatencyMode == false` 不能单独当作驱动限帧不可用的证据；低延迟 Off 不再伪装成 API 失败，遵守驱动／控制面板策略。
- [x] 实现明确的不可用／失败回退：切换到 Async 前清除应用设置的驱动限帧，保留同一个有效基础目标；避免双重限帧、重复错误日志和逐帧强制重试。清除失败必须明确报告，不能显示为已完成干净回退。
- [x] 处理 DXGI／DComp 切换、暂停恢复、源窗口重建、参数重启、停止及设备／会话失效，保证配置与帧身份不泄漏到新会话，停止不会被 Sleep 或队列等待卡住。
- [x] 保持显式 Async、Front Edge、帧同步关闭、普通非 FG Reflex，以及 XeSSFG／MFG 的 XeLL 职责清晰；不修改已有配置的模式含义来掩盖实际回退。

### 界面与说明

- [x] UI 分别显示用户请求模式、实际基础限帧器、Reflex 低延迟请求／查询状态和 FG 输出方式。只有驱动限帧路径实际启用时才显示“Reflex 驱动限帧”；回退时明确显示 Async 及原因。
- [x] 更新帧同步指南、模式对照表、Fix1 实施记录和双语更新说明，明确原 Beta 6 与 Fix1 的行为差异，不再把已有低延迟标记接入描述为新完成的驱动基础限帧。

### 验证与完成标准

- [x] 无界面测试覆盖生产模式解析与限帧配置、唯一限帧所有者、2×／3×／4× 换算、同候选一次 Sleep、生成／真实帧身份、Off 查询与 API 失败区别、清除限帧后回退、暂停恢复及并发停止。增加反向用例，确保“Reflex 请求仍解析为 Async”或“Async 与驱动限帧同时工作”会被检出。
- [x] 回归原 Beta 6 的全屏参数交接、FIFO 重试计时、槽位回收和资源复用；完成完整 Release 构建及对应包的版本／清单核对。
- [ ] 在获得明确同意后进行真实 NVIDIA 驱动对比：Reflex 与 Async 使用相同场景、目标和倍率，覆盖源 FPS 高低变化、GPU 满载、静止恢复、全屏／窗口、HDR 和捕获组合；核对真实基础帧率、生成有效率、排队帧龄、捕获后延迟和实际显示间隔。
- [x] 将“代码实现完成”“自动验证通过”“真实驱动行为通过”“性能收益得到验证”分别记录。实机未验证不作为停止实现的理由，也不能把自动测试结果当成驱动性能证明；未经用户明确同意，不自动操作应用或游戏。

完成标准：可用条件下选择 Reflex 确实进入驱动基础限帧路径，并有证据证明不存在同目标 Async 限帧；失败回退和界面状态准确。若验证暴露具体技术障碍，记录复现条件与阻塞点并继续处理，不能仅用“待验证”重新把功能降为永久 Async 映射。

## P0：全屏点击实时参数卡死／停止缩放

- [x] 将停止／源窗口重建改成持久的待处理请求，由外层循环完成；等待参数输入释放期间不丢失请求，不重复入队或刷日志。
- [x] 协调参数焦点交接与全屏源窗口状态检查，对短暂交接变化进行有界稳定检查；真正关闭、最小化等持久变化继续正确处理。
- [x] 保持 DLSSFG FIFO、待呈现计数、槽位回收和生产取消一致；未真正进入销毁前不得单独清空 FIFO。
- [x] 首次失败记录具体状态原因、前台／参数宿主 HWND、源窗口前后矩形及可见性／最小化状态。
- [x] 回归覆盖内部焦点、全屏／窗口源几何变化、鼠标按住后松开、短暂变化恢复、停止优先级及 FG 队列完成。

调查依据：[Beta 5 全屏参数停用报告](../reviews/20260910-beta5-fullscreen-parameter-stop.md)。

## P1：DLSSFG 调度与 Reflex 职责

- [x] 拆分 Reflex 的接口可用、请求结果与实际启用状态。分别记录 `SetSleepMode`、`GetSleepStatus` 返回值及 `bLowLatencyMode`；不再将“查询成功但状态为 Off”伪装为驱动返回的 `NVAPI_NOT_SUPPORTED`。区分真正调用失败、驱动未启用、呈现暂时不可用和停止；遵守控制面板覆盖，不强制绕过。
- [x] 修正 DLSSFG 呈现等待统计：当前 `pacingEnd = pacingStart` 导致 `pacingWait` 恒为零。累计同一 FIFO 任务从首次等待到完成／丢弃之间的调度等待，分别记录容量重试和 CPU 工作；GPU 执行耗时另由实机工具验证，避免重试重复计数。
- [x] 以“捕获前统一基础限帧 → Reflex Sleep → 最新有效画面 → NR/SR/光流/FG → 有界输出队列 → 呈现调度”为优先对比方案，减少 Front Edge 路径在效果处理完成后等待 FG 输入时刻造成的画面老化。保留兼容路径用于对照，不预先宣称延迟或吞吐量收益。
- [x] 基础 FPS 由单一调度入口负责。优先以 Async 为基线；Reflex 实际可用时默认低延迟 On、Boost Off，DLSSFG 的额外驱动限帧间隔暂保持 0。纯 Reflex 驱动限帧需独立验证 x2/x3/x4 单位和行为后再决定是否开放。
- [x] 保留 FG 真实帧／生成帧顺序和独立输出节奏；源捕获时间、实际处理开始、队列进入／离开、Present 时间分别记录，避免将处理或下游等待误当源帧间隔。
- [x] 队列满时在接受下一基础帧前反馈背压；补齐每张生成帧 fence 等待的验证构建统计，保留发布复制统计和原有资源保护。资源池约束已独立评估。
- [ ] 获得实机数据后，判断生成帧等待／发布复制是否为主要瓶颈，再决定是否实施有界独立资源上下文、GPU 端同步及并行度调整；不得直接删除共用 allocator、输出纹理及 SDK 插值禁用标志所需的等待。
- [x] UI 分别展示实际基础限帧方式、Reflex 请求／生效状态和 FG 输出方式；DLSSFG 选择 Reflex 时不得使用户误以为驱动正在负责限帧。

## P2：独立评估 Streamline 路线

- [x] 评估完整 `sl.dlss_g + sl.reflex` 接入与现有直接 NGX 路径的迁移成本，覆盖设备／交换链创建、资源标签、共享颜色与引导数据、帧身份、HDR、叠加层及重建。
- [x] 若采用 Streamline 托管呈现，明确 SDK 与应用对交换链等待对象和最大帧延迟的唯一所有权；不能只替换 Reflex 调用，也不能在现有直接 NGX 路径中假设已经存在 SL pacer。

## 验证要求

- [x] 完整 Release x64 构建。版本、清单、归档与配置保留核对结果记录在本地包的 `LOCAL-PREPARATION-AUDIT.json`。
- [x] 无界面测试覆盖状态区别、同一基础帧一次 Sleep、同目标单一限帧、失败回退、FIFO 重试计时、生成／真实帧 ID、资源复用与停止重建。
- [ ] 在获得用户明确同意后进行实机对比，覆盖 x2/x3/x4、源 FPS 高低变化、GPU 满载、静止恢复、全屏／窗口、不同刷新率、HDR 和捕获组合。
- [ ] 比较真实基础帧率、生成有效率、排队帧龄、捕获后延迟与实际显示间隔；Present 调用次数或平均 FPS 不能单独证明呈现质量。
- [x] 普通 Release 不增加逐帧文字日志或诊断性像素回读；必要 SDK 状态读取与诊断开销分开说明。

## 依据

- Beta 5 已确认、Beta 6 已修复的源码问题：`ReflexController.cpp` 将 `bLowLatencyMode == false` 转成 `-104`；`Renderer::RenderDLSSFGFrame` 的 `pacingWait` 恒为零。
- [NVAPI DirectX 文档](https://docs.nvidia.com/nvapi/group__dx.html)：实际低延迟状态可能与请求不同；低延迟和限帧独立；Sleep 的调用位置与频率。
- [Streamline Reflex 指南](https://github.com/NVIDIA-RTX/Streamline/blob/main/docs/ProgrammingGuideReflex.md)。
- [Streamline DLSS-G 指南](https://github.com/NVIDIA-RTX/Streamline/blob/main/docs/ProgrammingGuideDLSS_G.md)：SL pacer、等待对象所有权和显示间隔测量。该指南的托管呈现要求不直接等同于现有直接 NGX 实现。
