# 0.6.7-local 效果器选择器（r1 fix1）

在「效果组」中点击「添加效果器」，左侧按用途和子分组筛选，右侧搜索和选择单个效果器，底部通栏显示详情。选择器可超出主窗口，最大 820×800 DIP，按按钮所在显示器的工作区和 DPI 留出边距。采用 Magpie 现有 WinUI 主题的圆角 Flyout、背景材质、半透明分区和分隔线。

- 「入门」沿用原有的入门筛选；新增「进阶」快捷分类，包含 RTXVideo VSR、DLSS FG、XeSS FG 两个版本、DLSSNR 和 Frame Rate Filter，共 6 项。各效果器在原用途分类中也可找到。
- 搜索覆盖全部分类，匹配名称、用途、关键词和旧名称；点击分类会清空搜索。列表保留名称与一行用途说明。
- 点击主分组名称筛选整组并展开；点击固定的加减号只展开／收起。多个分组可以同时展开；在同一页面再次打开选择器时保留展开和分类状态。子分组被收起时，父组的选中标记与右侧分类路径仍提示当前筛选。
- 同族变体默认折叠：CuNNy（含原版／CuNNy2 两代）、NNEDI3、Anime4K、RAVU、SMAA、FXAA、xBRZ、FSRCNNX、CAS、FSR 1、NIS、XeSS 补帧。标题仅展开／收起，具体效果器才添加；筛选后只有一个成员时直接显示该成员。CRT、Sharpen 等不同算法集合不按文件夹合并。
- 家族仅显示当前筛选的成员，名称和数量随结果变化；变体采用自然数字排序。搜索临时展开匹配家族，可手动收起；清空后恢复普通浏览状态。
- 右侧 `A–Z／#` 在当前结果中跳转并聚焦，保留搜索与分类。固定为单列 27 行，顺序为 A 到 Z、最后 #；按钮 18×18 DIP，字号 10。没有结果的字母禁用，仅文字透明度降为 30%，按钮背景继续透明；可用项的悬停／按下反馈与圆角沿用主题。上下键跳过禁用字母。小显示器工作区限制高度时，保持同一列并允许滚动，不切换双列或字母弹出面板。数字、符号和非拉丁字母归入末尾的 `#`。
- 分类支持上下键导航、左右键展开／收起；效果器支持上下键导航和 Enter 添加。悬停或聚焦更新底部详情，移开后保留内容。
- 单击效果器立即追加到当前组末尾，继续使用原有拖动排序。添加前检查补帧冲突等组合限制；说明里提供下一步。
- 添加面板只选择效果器。RTX Video 强度通过效果组的参数按钮和工具栏参数面板调整。

## DLSS SR 与面板密度

DLSS SR 与 FSR2／3／4、XeSS SR 一同归入「放大与超分 → 时序重建」，显示名为 `DLSS SR`，保存 ID 仍为 `DLSS\DLSS_SR`。用途和组合建议围绕主要缩放步骤展开；保留原始相机抖动／真实深度缺失、可选估算运动、当前 Preset J 以及用户反馈的 L／M 轻微抗锯齿收益说明。

弹窗首选高度为 800 DIP，底部说明区固定为 200 DIP（含内边距和分隔线），说明长短不再改变上方两列高度。字号统一减少 1 DIP：名称和搜索 13、分类和正文 12、摘要 11、计数与索引 10、说明标题 15。普通按钮保留原有内边距；字母索引使用无内边距的 18 DIP 点击区域。

在选择器内按住 Ctrl 并滚动纵向滚轮，滚动当前下方说明；普通滚轮保留当前位置的控件行为。说明无滚动空间或已到顶／底时，Ctrl＋滚轮仍只作用于说明，保持两列浏览位置。支持反向和高精度滚轮，选择器的滚动控件禁用缩放。

仅在标题和正文超出说明区时，右下角显示「Ctrl + 滚轮：滚动说明」。提示位于独立的底行，保留正文滚动空间，不覆盖文字；切换到可完整显示的短说明时隐藏，切换说明后滚动位置回到顶部。溢出判定使用未扣除提示行的可用高度，避免提示自身造成不必要的滚动。窗口尺寸或文本布局变化后更新提示，鼠标实际移动和键盘聚焦继续更新当前说明。

三区使用同一主题基色的递增半透明覆盖：分类最轻、效果器居中、说明最明显。深色主题由深到浅，浅色主题由浅到深；圆角、分隔线和 Acrylic 材质沿用 Magpie 控件。

## RTX Video

正式 ID 为 `RTXVideo\RTXVideo_Denoise`、`RTXVideo\RTXVideo_VSR`；`strength` 参数值 0／1／2／3 对应低／中／高／极高，新添加默认 1（中档）。Denoise 保持输入尺寸，VSR 使用配置的输出尺寸。

八个旧档位 ID 在配置读取和效果组导入的共同路径中精确迁移，保留原档位、位置、尺寸与其他参数。再次加载不重复迁移；之后保存／导出使用新形式。原配置在迁移写入前由现有配置备份机制保存。旧名字仍可搜索，源码与发布包改为 155 个 HLSL、155 个正式选择入口；原 161 项 review 保留为历史资料。

运行中切档在后台渲染线程、两帧之间创建并加载候选模型，沿用已同步的输入／输出资源。加载成功后替换句柄；失败保留原模型，回退相应参数并提示尝试较低档位或提供日志。回退会核对当前值，避免覆盖之后的修改。工具栏分别显示保存状态、正在应用和失败回退；实时应用中的值不会被计作需要重启的变化。

`NvVFX_Load` 可能产生短暂停顿，候选模型也会临时增加显存占用。本实现没有承诺无中断热切换；具体驱动下的四档画质、时延和资源占用由用户验收。

## DLSSNR 自动 HDR

当前 `067 local` 的旧 HDR 开关继续隐藏并在加载时关闭。新增「HDR 组件」主分类：HDR → SDR、SDR → HDR、RTX Video HDR。组件决定各节点的实际颜色域，NR 根据自身输入选择 SDR／HDR 路径；例如放在 HDR → SDR 后面时使用 SDR。见 [实施记录与使用组合](../reviews/20260908-v0.6.7-hdr-conversion-effects-plan.md)。

参数按钮和工具栏已移除 `HDR Processing Path`、`Experimental HDR Scale` 及对应空分组；旧配置中的这两个值在读取／导入时移除，保留风格、细节与运动参数。

HDR 关闭时沿用 SDR 路径。HDR 开启时，在效果资源创建前核对输入是否经过捕获层的 canonical 转换、颜色信息是否有效、后端 FP16 能力是否可用；满足条件后使用 FP16 边界，倍率固定为中性的 1。输入和输出转换分别除以、乘以当前 SDR 白点与 80 nit 的比值。原始纹理是 FP16 本身不会触发该路径。

捕获层对缺失颜色信息已有明确的 SDR 默认描述，记录 inferred 信息后再归一化；白点和颜色边界随输入更新，格式在一个运行会话中保持稳定，尺寸变动由现有 Resize 流程重建资源。未新增逐帧亮度分析或自动猜测实验倍率。

FP16 路径的高光、颜色和实际 AI 观感仍需验证；该路径下现有输入分辨率调整／残差重建不可用，工具栏将相关参数标为不可用。其他风格参数保留。未验证 NVIDIA 的私有模型归一化曲线；边界转换的数值往返通过不等于模型无损。

## 验证与维护

- `tests/Run-EffectPickerR1Tests.ps1 -CoreLibrary <Release Magpie.Core.lib>`：搜索、分类、精确别名、八档迁移、幂等性、参数保留、默认／无效值、旧请求回退保护、自动 HDR 输入／能力判定，以及独立进程中的真实 XAML 集合和三分区布局（820×800、820×640、480×360、280×220）。
- `tests/Run-EffectPickerFix1Tests.ps1`：以上回归，以及家族／代际分组、过滤后的精确 ID、自然排序、索引、隐藏 0／1／10／100 行的间距、首尾／全隐藏／单结果、加减号几何、多 DPI 尺寸。加载生产 XAML 样式到同版本 WinUI 2 的独立无窗口进程，检查深浅主题、圆角、半透明画刷和默认 Flyout 模板。回归检查进阶分类的精确 ID、155 项正文在两种宽度下仍保持 200 高度、右下角提示不覆盖正文、长短说明切换、Ctrl／普通／横向滚轮判定及正反向、高精度和边界偏移，以及三区主题覆盖强度的顺序。无窗口测试不模拟实际鼠标、焦点或滚动事件；实际 Ctrl＋滚轮跨区域操作、列表位置不变、说明重置、屏幕边界和主题观感由用户验收。
- `tests/Validate-EffectCatalog.ps1`：155 个源文件与目录一致、两项 RTX 参数定义、旧名称搜索别名。
- `EffectPickerThemeTests` 同时检查字母按钮的 18 DIP 尺寸、系统焦点、深浅主题，以及从悬停／按下转为禁用时仅文字透明度降低、背景透明、恢复可用后文字复原。
- `scripts/tests/hdr_surface_warp.cpp`：提取生产转换着色器，在软件 D3D11 WARP 上验证 HDR 映射，以及 80／203／360 nit 白点下的自动 DLSSNR 边界往返。此测试不调用 NVIDIA 效果器。
- Release 构建和运行包统一使用 `release/v0.6.7-local/Magpie-Experimental-x64`；中间文件统一在版本 `obj/`，不建立 `.build`。`Deploy-Local067.ps1` 从已提交源码构建、检查内置效果文件并生成清单；保留配置、日志与自定义文件。八个旧 RTX 文件在构建验证后备份至版本目录的 `retired-effects` 再移除。

说明来源：[原始 review](../reviews/20260908-effect-content-catalog.json)；产品目录由 `scripts/Generate-EffectCatalog.ps1` 生成并嵌入 EXE。r1 将 RTX 元数据合并为两项，更新 DLSSNR 自动路径说明。

实际 Magpie UI、NVIDIA GPU 效果与画质由用户自行测试。建议重点检查：点击添加及参数按钮，树形分类与搜索，RTX VSR／Denoise 四档切换及失败回退，旧组重开／导入后的档位和尺寸。HDR 组件接入后共 158 项；旧 HDR 开关仍隐藏。HDR 捕获需要 Graphics Capture 和源屏幕开启 Windows HDR，HDR 输出需要目标屏幕开启 Windows HDR。检查普通转换的配对／重启、RTX HDR 四项参数实时应用、原画对比、NR 所在颜色段及补帧组合的亮度与停止操作。

## 2026-09-08 回归修复

用户报告参数设置失效、RTX 强度显示为空白，以及打开选择器闪退。日志错误为 `0x80070057`，同次转储为 `Windows.UI.Xaml.dll` 的未处理异常 `0xc000027b`。转储未包含原始 stowed exception 堆内存；具体触发点通过独立 XAML 控件测试复现。

根因：新增强度控件把 `single_threaded_vector<hstring>` 交给 XAML `ItemsSource`，该集合没有 XAML 需要的 `IInspectable` 集合接口。设置时抛出参数异常。在效果器行模板中，这一步排在 `HasParameters` 和 `Parameters` 绑定之前，会中断后续初始化；在选择器构建中，同一错误传播出点击事件并导致退出。

修复：统一使用可供 XAML 绑定的集合，RTX 强度纳入现有参数组；打开选择器的局部异常会记录错误并提示重试，保留效果组。`tests/EffectChoiceItemsTests.cpp` 的 `--xaml` 测试在无可见窗口的独立进程中验证真实 `ComboBox`：旧集合抛 `E_INVALIDARG`，新集合正常选择四档并读取选中值。未操作用户界面，也未启动缩放或 GPU 效果器测试。

## DLSS NR／FG 光流选择

参数按钮和工具栏均提供「无／AMDOF／NVOF」；AMDOF 为性能／质量两档，NVOF 为原有五档。新添加效果默认 NVOF 均衡，旧效果保留原关闭状态或 NVIDIA 档位。提供者与质量修改需使用工具栏的应用并重新启用操作。

同组多个消费者共用一次光流计算；设置不同时沿用 NVOF 优先、同提供者取最高请求质量的规则，并提示实际配置。要比较 AMD 与 NVIDIA，请把组内启用光流的效果设为相同提供者。重点测试 NR 单独、FG 单独、NR+FG、NR 缩小输入、快速运动及停止／恢复；AMD 性能档应正常启用光流。
