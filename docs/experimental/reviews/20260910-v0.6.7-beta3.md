# 0.6.7 Beta 3：名称、NR 参数与关于入口

- 性能分析器和工具栏复用的显示函数为 XeSS FG/MFG 增加与主界面一致的显示别名，内部 ID 和文件名保留旧值以兼容配置。
- NR intensity、localToneStrength、localStructureStrength 的 HLSL 描述、运行参数解析、初始化和效果组导入范围均改为 0–2；默认 1、步进 0.05、实时更新方式保持一致。
- 关于页面四个入口使用 SAOG0721/Magpie；FAQ 与贡献指南按当前界面语言选择中文或英文，简繁中文均使用中文文件。
- 中英文 FAQ 修正补帧与超分辨率说明，实际 GitHub 文档在源码同步后生效。

`tests/Run-Beta3Tests.ps1` 提取实际生产函数，检查参数边界、非有限值、默认值、导入和初始化不截断 1–2、显示别名和自定义名称；另核对参数元数据、实时更新入口和 XAML 链接。测试通过，实际 NR 1–2 画质与 UI 由维护者验收。
